mirrors:
  buildcache-source:
    fetch: ${PROTECTED_MIRROR_FETCH_DOMAIN}/testing/SPACK_REPLACE_VERSION/${SPACK_CI_STACK_NAME}
    push: ${PROTECTED_MIRROR_PUSH_DOMAIN}/testing/SPACK_REPLACE_VERSION/${SPACK_CI_STACK_NAME}
    source: False
    binary: True
  buildcache-destination:
    fetch: ${PROTECTED_MIRROR_FETCH_DOMAIN}/testing/${CI_COMMIT_REF_NAME}/${SPACK_CI_STACK_NAME}
    push: ${PROTECTED_MIRROR_PUSH_DOMAIN}/testing/${CI_COMMIT_REF_NAME}/${SPACK_CI_STACK_NAME}
    source: False
    binary: True
