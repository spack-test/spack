mirrors:
  buildcache-destination:
    fetch: ${PR_MIRROR_FETCH_DOMAIN}/testing/${CI_COMMIT_REF_NAME}/${SPACK_CI_STACK_NAME}
    push: ${PR_MIRROR_PUSH_DOMAIN}/testing/${CI_COMMIT_REF_NAME}/${SPACK_CI_STACK_NAME}
    source: False
    binary: True
